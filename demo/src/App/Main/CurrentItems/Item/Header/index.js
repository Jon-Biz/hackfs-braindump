import React from 'react'
import HeaderMenu from './Menu'

const Header =
        () => (
            <div>
                <HeaderMenu />                
                <input type="text" value="Title" />
            </div>
        )  

export default Header